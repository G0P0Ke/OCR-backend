spring:
  liquibase:
    change-log: classpath:db/changelog/liquibase-changelog.xml
    enabled: true
  datasource:
    url: "jdbc:postgresql://db/ocrdatabase?ApplicationName=ocr_backend"
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
